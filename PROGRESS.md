# Ход работ

## Итерация 1 — дизайн-система и главная страница

**Статус:** главная страница готова, проверки пройдены. Ожидает визуальной приёмки.

### Что сделано

| Этап | Результат |
| --- | --- |
| Диагностика | Каркас Next 16.3.1 / React 19.2.8 / TS / Tailwind 4 на месте, `legacy/` цел |
| Зависимости | `zod` (прод), `sharp` (dev, обработка изображений) |
| Дизайн-система | Токены палитры, Prata + Manrope, сетка, кнопки, поля, бейджи, состояния |
| Данные | `company.ts`, `navigation.ts`, `founders.ts`, `src/content`, типы, `.env.example` |
| Владельцы | `FoundersSection`, пять пропорций, заглушки вместо непригодного исходника |
| Главная | 14 блоков от служебной строки до подвала |
| Проверки | lint ✅, typecheck ✅, build ✅ |

### Проверки

```
npm run lint     ✅ без замечаний
npx tsc --noEmit ✅ без ошибок
npm run build    ✅ / , /brand-preview , /_not-found — статика
```

Дополнительно проверено, что фирменные классы и токены реально попали
в собранный CSS (`--color-red-brand`, `container-page`, `rule-foil`),
а шрифты скачаны локально — 10 файлов `.woff2` в сборке.

### Не сделано (следующие итерации)

- Каталог `/catalog`, категории, карточка товара
- Корзина и checkout
- Оптовый раздел и адаптер «МойСклад»
- Корпоративные, доставка, контакты, о компании, уход, юридические
- Аналитика, sitemap, robots
- Тесты (Vitest, Playwright)

### Решения, которые стоит знать

1. **Переход на Next.js обоснован технически.** Статический `index.html`
   не даёт корзины, серверных экшенов и интеграции с «МойСклад».
   Прежний сайт целиком сохранён в `legacy/`.

2. **Цены не заполнены.** Компания их не передавала, придумывать нельзя.
   `price: null` выводится как «Цена уточняется», кнопка — «Узнать цену».
   Правится в `src/content/catalog.ts`.

3. **Отзывов нет.** Массив в `src/content/reviews.ts` пуст, блок показывает
   честное пустое состояние вместо выдуманных имён и оценок.

4. **Фотография владельцев не подошла.** Подробности — в
   `founders-source/README.md`. На сайте стоят фирменные заглушки.

---

## Итерация 2 — P0: публикация внутренних страниц

**Статус:** выполнено. Внутренних 404 не осталось.

### Что сделано

Проведён read-only аудит, результат — `AUDIT_BEFORE_CHANGES.md`.

Создана 21 страница вместо двух:

| Раздел | Адреса |
| --- | --- |
| Каталог | `/catalog`, `/catalog/[category]` (6), `/product/[slug]` (11) |
| Заказ | `/cart`, `/checkout`, `/order/success` |
| Опт | `/wholesale`, `/wholesale/catalog` |
| Компания | `/about`, `/contacts`, `/delivery`, `/care`, `/corporate` |
| Клиентская база | `/reminders`, `/unsubscribe` |
| Юридические | `/privacy`, `/consent`, `/offer` |
| Служебные | `/sitemap.xml`, `/robots.txt`, 404 |

Убраны иконки поиска, избранного и корзины из шапки: они вели в пустоту.
В мобильной панели корзина заменена на магазины.

Добавлен `scripts/check-links.mjs` — обходит статическую выгрузку и падает,
если найдена битая внутренняя ссылка. Подключён в CI перед публикацией.

### Проверки

```
npm run lint         ✅
npm run typecheck    ✅
npm run build:pages  ✅ 37 страниц
npm run check:links  ✅ 946 ссылок, битых нет
```

### Осознанные ограничения

1. **Корзина и оформление не работают.** Складывать в корзину товары без цен
   бессмысленно, поэтому страницы существуют, но честно объясняют, что заказ
   принимает флорист. Механика — этап P2 вместе с ценами.
2. **Оптовые остатки не выводятся.** «МойСклад» не подключён, а показывать
   выдуманные позиции нельзя: оптовый клиент примет их за настоящие.
3. **Форма важных дат не принимает данные.** Хранить телефоны можно только
   на сервере с согласиями — этап P3. Пока дату принимает менеджер.
4. **Юридические страницы — шаблоны.** Реквизиты, ИНН и ОГРН не переданы,
   документы требуют проверки юристом. Это помечено на самих страницах.

### Дальше — P1: визуал

Палитра с крафтом, иконка «Ц», новая шапка, первый экран без сплошного
красного, карточки, сокращённый подвал.

---

## Итерация 3 — P1: визуальный ребрендинг

**Статус:** выполнено, опубликовано.

### Палитра

Ярко-красный `#B10F24` удалён из кода полностью. Основа — бургунди
`#741C2C` как акцент, шампань и тёплая бумага как основные поверхности,
крафт как материал, золото только на тонких линиях. Токены переименованы
по всему коду в 36 файлах. Крафтовая фактура — встроенный SVG-шум,
ни одного растрового файла.

### Монограмма «ЦБД»

Три равноправные концепции с единой метрикой: архитектурная, лента
и монолит. Все контуры — собственные paths. На сайте временно стоит
архитектурная — окончательный выбор за владельцем, сравнение на
`/brand-preview` с носителями: коробка, лента, бирка, пакет, вывеска,
аватар, фургон.

Для 16 и 24 px используется отдельная micro-версия: полная монограмма
в этих размерах нечитаема — проверено рендером.

Иконки собираются скриптом из того же файла, что и знак на сайте,
поэтому разойтись они не могут: favicon 16/32/48, Apple Touch Icon,
PWA 192/512, maskable, аватар 1080, Open Graph 1200×630.

### Неподтверждённые условия

Вынесены в `src/config/claims.ts` со статусом `TODO_CONFIRM` и в
интерфейс не попадают: способы оплаты, возврат денег, замена букета,
тарифы доставки, минимум оптового заказа. Юридические страницы —
`noindex` и вне sitemap. Сервис важных дат помечен «Скоро», кнопка
отключена, данные нигде не сохраняются.

### Проверки

```
npm run lint         ✅
npm run typecheck    ✅
npm run build:pages  ✅ 37 HTML-страниц
npm run check:links  ✅ 1042 ссылки, битых нет
```

Ширины 320, 390 и 768 px проверены рендером в iframe фиксированной
ширины: обычный headless-снимок оказался недостоверным — браузер
рендерит в десктопном вьюпорте и кадрирует изображение.

### Осталось на следующие этапы

- выбор владельцем финальной концепции монограммы;
- профессиональная съёмка владельцев и единый фотостандарт товаров;
- P2: цены, размеры, корзина, оформление заказа;
- P3: важные даты, согласия, amoCRM, Wazzup;
- P4: остатки из «МоегоСклада».

---

## Итерация 4 — визуальная доводка после публикации

**Статус:** выполнено, опубликовано.

| Замечание | Что сделано |
| --- | --- |
| Пустая рамка на `/wholesale` | Убрана рамка контейнера, под фото — крафтовая подложка того же размера. Блок стоит высоко на странице, поэтому фотография грузится сразу, а не лениво. |
| Скелетоны в каталоге | Карточки рендерит сервер и они есть в HTML сразу. Клиентский компонент отвечает только за фильтры: читает повод из адреса и прячет неподходящие карточки по data-атрибуту — фотографии не перезагружаются. |
| Маркировка `D&F` на первом экране | Фотография заменена на «Нежный розовый» — без чужой и старой маркировки, тёплая по цвету, ложится на шампань и бургунди. |
| Три логотипа не посмотреть | Создана публичная `/logo-preview/` — `noindex`, вне sitemap и меню, открывается по прямой ссылке. |
| Знак выглядит технично | Добавлена концепция **D · Пластика**: тоньше штрих, скруглённые торцы и сгибы, у «Д» плечи дугами. Мягче архитектурной и ближе к премиальной подписи бренда. |

### Проверки

```
npm run lint         ✅
npm run typecheck    ✅
npm run build:pages  ✅ 38 HTML-страниц
npm run check:links  ✅ 1109 ссылок, битых нет
```

### Замечание по методике

Обычный headless-снимок оказался недостоверным: браузер рендерит страницу
в десктопном вьюпорте и кадрирует изображение, из-за чего мобильная версия
выглядела обрезанной. Все проверки узких ширин делаются через iframe
фиксированной ширины.
